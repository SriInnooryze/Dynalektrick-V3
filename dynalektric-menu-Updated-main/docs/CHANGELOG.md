# Dynalektric Changelog

## [1.0.0-enterprise] - 2026-08-07

### Added
- Enterprise folder structure (`src/`, `public/`, `scripts/`, `docs/`, `tests/`, `.github/`).
- Service Layer (`src/services/api.js`, `contactService.js`, `exportService.js`).
- Lib Layer (`src/lib/dom`, `browser`, `performance`, `storage`).
- Adapters Layer (`src/adapters/analytics`, `forms`, `images`, `export`).
- Feature Modules (`src/features/products`, `industries`, `export`, `contact`).
- Performance Architecture (`src/performance/lazy`, `preload`, `observers`, `metrics`).
- Centralized Logger (`src/logger/logger.js`).
- Centralized Error Handling (`src/errors/ErrorBoundary.jsx`, `errorHandler.js`).
- Comprehensive Documentation Suite & ADRs in `docs/`.
- CI/CD Workflows (`.github/workflows/build.yml`, `lint.yml`, `deploy.yml`).

### Preserved
- 100% UI, UX, Layout, Styling, Animations, SEO Metadata, HTML Structure, Prerender Output, and JavaScript Runtime Behavior.
